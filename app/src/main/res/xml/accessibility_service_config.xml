<?xml version="1.0" encoding="utf-8"?>
<!--
  canRetrieveWindowContent + canPerformGestures are what let this act like a screen
  automator (read the node tree, click/scroll/long-press) without root.
  packageNames scopes it to Google Messages only, so it won't see/touch anything else.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:canPerformGestures="true"
    android:notificationTimeout="100"
    android:packageNames="com.google.android.apps.messaging" />
